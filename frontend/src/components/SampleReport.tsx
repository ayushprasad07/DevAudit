import { ArrowUpRight, CircleAlert, CircleCheck, CircleDashed } from "lucide-react";
const findings = [["lodash", "4.17.15", "CVE-2021-23337", "Update required", "critical"], ["legacy-utils", "1.8.0", "GPL-3.0", "License review", "review"], ["pymongo", "4.6.2", "Apache-2.0", "Clear", "clear"]];
export default function SampleReport() { return <section className="section shell report-layout" id="report"><div className="report-copy"><p className="eyebrow">A useful answer, not more data</p><h2>Know what to fix<br /><em>and why it matters.</em></h2><p>Every finding includes the package, the rule behind it, the severity, and a crisp next action—so the conversation can move straight to resolution.</p><a className="text-action" href="#start">See what an audit sees <ArrowUpRight size={17} /></a></div><div className="report-window"><div className="window-bar"><div><i /><i /><i /></div><span>devaudit / release-readiness</span><CircleDashed size={15} /></div><div className="window-summary"><span>Production readiness</span><strong>2 actions required</strong><small>Scan completed · just now</small></div><div className="finding-list">{findings.map(([name, version, signal, action, state]) => <div className="finding" key={name}>{state === "critical" ? <CircleAlert size={18} /> : state === "clear" ? <CircleCheck size={18} /> : <CircleDashed size={18} />}<div><strong>{name} <small>{version}</small></strong><span>{signal}</span></div><b className={state}>{action}</b></div>)}</div><div className="window-footer"><span>41 packages scanned</span><a href="#start">Open report <ArrowUpRight size={14} /></a></div></div></section>; }
